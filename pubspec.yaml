name: voice_ludo_masti
description: A polished local multiplayer Ludo game with instant voice-controlled dice.
publish_to: "none"
version: 1.3.0+11

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
