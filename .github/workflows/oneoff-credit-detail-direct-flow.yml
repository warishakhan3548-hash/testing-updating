name: One-off Credit Detail Direct Given Taken

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/oneoff-credit-detail-direct-flow.yml"

permissions:
  contents: write

jobs:
  migrate-verify-commit:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout current main
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Apply Credit detail direct Given Taken migration
        run: python3 .github/credit_detail_direct_flow_patch.py

      - name: Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Flutter stable
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.47.2"
          channel: stable
          cache: true

      - name: Resolve packages
        run: flutter pub get

      - name: Format migrated source
        run: dart format lib/main.dart

      - name: Analyze
        run: flutter analyze

      - name: Test ledger invariants
        run: flutter test

      - name: Build Android debug APK
        run: flutter build apk --debug

      - name: Remove one-off migration machinery
        run: |
          rm -f .github/credit_detail_direct_flow_patch.py
          rm -f .github/workflows/oneoff-credit-detail-direct-flow.yml

      - name: Commit verified production Credit modal UI
        run: |
          git config user.name "Aarish UI Migration Bot"
          git config user.email "actions@users.noreply.github.com"
          git add -A
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "No migration changes to commit."
            exit 0
          fi
          git commit -m "Simplify credit detail to direct Given Taken actions [skip ci]"
          git push origin HEAD:main
